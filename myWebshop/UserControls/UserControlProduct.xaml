<UserControl x:Class="myWebshop.UserControls.UserControlProduct"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:myWebshop.UserControls">
    <StackPanel>
        <DataGrid
            x:Name="dataGridProducts"
            Width="1100"
            Height="600"
            AutoGenerateColumns="False"
            CanUserAddRows="False"
            CanUserDeleteRows="False"
            CanUserResizeColumns="False"
            IsReadOnly="True"
            SelectionChanged="dataGridProducts_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Product}" Header="Felhasználó név" />
                <DataGridTextColumn Binding="{Binding Jelszo}" Header="Jelszó" />
                <DataGridTextColumn Binding="{Binding TeljesNev}" Header="Teljes név" />
                <DataGridTextColumn Binding="{Binding Szerepkor}" Header="Szerepkör" />
                <DataGridTextColumn Binding="{Binding SzerepkorNev}" Header="Szerepkör név" />
            </DataGrid.Columns>
        </DataGrid>
    </StackPanel>
</UserControl>
