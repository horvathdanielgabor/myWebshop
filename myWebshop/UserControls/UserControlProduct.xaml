<UserControl x:Class="myWebshop.UserControls.UserControlProduct"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:myWebshop.UserControls">
    <StackPanel>
        <DataGrid
            x:Name="dataGridProducts"
            Width="1100"
            Height="600"
            AutoGenerateColumns="False"
            CanUserAddRows="False"
            CanUserDeleteRows="False"
            CanUserResizeColumns="False"
            IsReadOnly="True"
            SelectionChanged="dataGridProducts_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Name}" Header="Termék neve" />
                <DataGridTextColumn Binding="{Binding Price}" Header="Ár (Ft)" />
                <DataGridTextColumn Binding="{Binding StockQuantity}" Header="Mennyiség" />
                <DataGridTextColumn Binding="{Binding Category}" Header="Kategória(k)" />
                <DataGridTextColumn Binding="{Binding Brand}" Header="Márka" />
                <DataGridTextColumn Binding="{Binding Weight}" Header="Súly (g)" />
                <DataGridTextColumn Binding="{Binding IsAvailable}" Header="Elérhető-e" />
                <DataGridTextColumn Binding="{Binding CreatedAt}" Header="Megjelenés dátuma" />
            </DataGrid.Columns>
        </DataGrid>
    </StackPanel>
</UserControl>
